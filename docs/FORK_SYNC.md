# Fork 仓库自动同步指南

如果你 Fork 了本仓库，可以使用 GitHub Actions 自动同步上游更新。

## 🚀 快速开始（Fork 用户）

### 1. 启用 GitHub Actions

Fork 仓库后，需要手动启用 GitHub Actions：

1. 访问你的 Fork 仓库
2. 点击 **Actions** 标签页
3. 点击 **I understand my workflows, go ahead and enable them**

### 2. 配置自动同步

你的 Fork 仓库已经包含了同步工作流（`.github/workflows/sync-fork.yml`），有三种方式触发同步：

#### 方式 1: 定时自动同步
- 默认每天 UTC 8 点自动检查更新
- 如果有新版本，会自动合并并推送

#### 方式 2: 手动触发
1. 进入 **Actions** 标签页
2. 选择 **Sync Fork** 工作流
3. 点击 **Run workflow**
4. 选择是否强制同步（覆盖本地修改）
5. 点击 **Run workflow**

#### 方式 3: 上游更新通知（需要配置）
当原仓库有更新时，可以自动触发你的同步：
- 需要在原仓库配置 Webhook（需要管理员权限）

## 🔄 同步流程

### 自动同步时会发生什么：

```
1. 检查上游仓库 (wu529778790/navhub.shenzjd.com)
2. 比较提交差异
3. 如果有更新：
   - 拉取上游代码
   - 合并到你的 main 分支
   - 推送到你的仓库
4. 如果有冲突：
   - 标记为失败
   - 需要手动解决
```

### 手动强制同步：

如果选择强制同步（force=true）：
- 使用上游版本覆盖所有冲突
- 适合想要完全同步上游的情况

## ⚠️ 常见问题

### Q: 同步时出现冲突怎么办？

**A:** 有两种解决方案：

**方案 1: 手动解决（推荐）**
```bash
# 在本地克隆你的仓库
git clone https://github.com/你的用户名/navhub.shenzjd.com.git
cd navhub.shenzjd.com

# 拉取最新代码
git pull upstream main

# 解决冲突（编辑冲突文件）
# 然后提交
git add .
git commit -m "resolve merge conflicts"
git push origin main
```

**方案 2: 强制使用上游版本**
- 在手动触发工作流时选择 `force: true`
- 会使用上游版本覆盖所有修改

### Q: 我的自定义修改会被覆盖吗？

**A:** 取决于修改的位置：

| 修改类型 | 同步后结果 | 建议 |
|---------|-----------|------|
| 修改配置文件 | 可能被覆盖 | 使用环境变量或单独配置文件 |
| 修改 UI 样式 | 可能被覆盖 | 考虑提交 PR 到上游 |
| 添加新功能 | 不会被覆盖 | 可以安全保留 |
| 修改环境变量 | 不会被覆盖 | 安全 |

### Q: 如何保留我的自定义配置？

**A:** 建议：
1. 环境变量放在 `.env.local`（已加入 .gitignore）
2. 个性化配置放在单独文件
3. 如需修改核心代码，考虑提交 PR 到上游

### Q: 可以关闭自动同步吗？

**A:** 可以，有几种方式：

**方式 1: 禁用工作流**
- 进入 Actions 标签页
- 找到 Sync Fork
- 点击 **Disable workflow**

**方式 2: 删除工作流文件**
```bash
git rm .github/workflows/sync-fork.yml
git commit -m "禁用自动同步"
git push
```

### Q: 如何知道同步是否成功？

**A:** 查看方式：
1. **Actions 标签页** - 查看工作流运行历史
2. **Commit 记录** - 查看是否有新的合并提交
3. **邮件通知** - GitHub 会发送同步结果通知

## 🛠️ 高级配置

### 修改同步频率

编辑 `.github/workflows/sync-fork.yml`：

```yaml
schedule:
  - cron: '0 8 * * *'  # 每天 8 点 UTC
  # 可以修改为：
  # '0 */6 * * *'  # 每6小时
  # '0 0 * * 0'    # 每周日
```

### 添加同步状态徽章

在你的 README.md 中添加：

```markdown
![Sync Status](https://github.com/你的用户名/navhub.shenzjd.com/workflows/Sync%20Fork/badge.svg)
```

### 手动同步脚本

如果不想用 GitHub Actions，可以在本地运行：

```bash
#!/bin/bash
# sync.sh

echo "🔄 同步上游更新..."

# 添加上游（如果还没有）
git remote add upstream https://github.com/wu529778790/navhub.shenzjd.com.git 2>/dev/null || true

# 拉取上游
git fetch upstream

# 合并
git merge --no-edit upstream/main

# 推送
git push origin main

echo "✅ 完成！"
```

## 📋 最佳实践

### 1. 定期同步
- 保持与上游同步，获取最新功能和修复

### 2. 备份配置
- 在同步前备份你的配置文件
- 记录你修改过的文件

### 3. 测试环境
- 先在测试分支同步
- 确认无误后再合并到 main

### 4. 关注上游更新
- 查看上游的 CHANGELOG 或 Release
- 了解重大变更

## 🔗 相关链接

- **上游仓库**: https://github.com/wu529778790/navhub.shenzjd.com
- **Issues**: https://github.com/wu529778790/navhub.shenzjd.com/issues
- **Discussions**: https://github.com/wu529778790/navhub.shenzjd.com/discussions

## 💡 提示

### 如果你做了大量自定义修改：

1. **考虑创建独立分支**
   ```bash
   git checkout -b custom-features
   # 在此分支上做修改
   ```

2. **定期合并上游**
   ```bash
   git checkout main
   git pull upstream main
   git checkout custom-features
   git rebase main
   ```

3. **贡献回上游**
   - 如果你的修改对其他人也有用
   - 考虑提交 Pull Request

## 🆘 需要帮助？

如果同步过程中遇到问题：

1. 查看 Actions 详细日志
2. 阅读本项目的 Issues
3. 在上游仓库提交 Issue
4. 参考 GitHub 文档：[Syncing a fork](https://docs.github.com/en/pull-requests/collaborating-with-pull-requests/working-with-forks/syncing-a-fork)
