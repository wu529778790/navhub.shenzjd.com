# GitHub OAuth 配置
# 在 https://github.com/settings/applications 创建 OAuth App
# 注意：Authorization callback URL 必须填写: http://localhost:3000/auth/github/callback
NEXT_PUBLIC_GITHUB_CLIENT_ID=your_github_client_id
GITHUB_CLIENT_SECRET=your_github_client_secret

# GitHub 仓库配置（可选）
# 如果你要 fork 其他人的项目，需要配置这些
NEXT_PUBLIC_GITHUB_OWNER=your_github_username
NEXT_PUBLIC_GITHUB_REPO=nav.shenzjd.com

# 本地开发时，确保 OAuth App 的回调 URL 包含:
# - http://localhost:3000/auth/github/callback (开发环境)
# - https://your-domain.com/auth/github/callback (生产环境)
